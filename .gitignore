# dependencies
node_modules/

# build output
dist/
build/

# env & secrets
.env
.env.local
.env.*.local

# logs
*.log
npm-debug.log*

# prisma
prisma/*.db

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/

# Firebase service-account keys — these can send push as this app. Never commit.
# google-services.json is NOT here on purpose: it is client config, ships inside
# the APK, and EAS needs it at build time.
*serviceAccount*.json
*service-account*.json
firebase-adminsdk*.json
matchventure-*-firebase-adminsdk-*.json
